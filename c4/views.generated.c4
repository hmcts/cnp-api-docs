// Generated by bin/generate-c4-views.mjs. Do not edit.
// Views with hand-written actor detail are in c4/views.c4.

views {

  view landscape {
    title 'CNP landscape'
    description 'Traffic between products. Authentication and identity are excluded. Click a product to open its own view.'
    include
      element.kind = product,
      element.kind = product -> element.kind = product
    exclude
      element.tag = #ambient,
      HMI_Gateway,
      CaTH,
      SNL,
      WA,
      DARTS,
      PRE
    autoLayout TopBottom 60 40

    style element.tag = #platform {
      color green
    }
  }

  view am_overview of AM {
    title 'AM overview'
    description 'Services in AM and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view bar_fee_pay_overview of BAR__Fee___Pay {
    title 'BAR, Fee & Pay overview'
    description 'Services in BAR, Fee & Pay and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view bsp_overview of BSP {
    title 'BSP overview'
    description 'Services in BSP and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view cath_overview of CaTH {
    title 'CaTH overview'
    description 'Services in CaTH and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view ccd_overview of CCD {
    title 'CCD overview'
    description 'Services in CCD and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view cmc_overview of CMC {
    title 'CMC overview'
    description 'Services in CMC and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view div_overview of DIV {
    title 'DIV overview'
    description 'Services in DIV and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view et_overview of ET {
    title 'ET overview'
    description 'Services in ET and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view evidence_overview of Evidence {
    title 'Evidence overview'
    description 'Services in Evidence and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view finrem_overview of FINREM {
    title 'FINREM overview'
    description 'Services in FINREM and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view fpla_overview of FPLA {
    title 'FPLA overview'
    description 'Services in FPLA and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view hmi_gateway_overview of HMI_Gateway {
    title 'HMI Gateway overview'
    description 'Services in HMI Gateway and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view ia_overview of IA {
    title 'IA overview'
    description 'Services in IA and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view idam_overview of IDAM {
    title 'IDAM overview'
    description 'Services in IDAM and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view lau_overview of LAU {
    title 'LAU overview'
    description 'Services in LAU and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view pcq_overview of PCQ {
    title 'PCQ overview'
    description 'Services in PCQ and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view pre_overview of PRE {
    title 'PRE overview'
    description 'Services in PRE and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view rd_overview of RD {
    title 'RD overview'
    description 'Services in RD and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view rpa_overview of RPA {
    title 'RPA overview'
    description 'Services in RPA and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view rpe_overview of RPE {
    title 'RPE overview'
    description 'Services in RPE and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view snl_overview of SNL {
    title 'SNL overview'
    description 'Services in SNL and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view sscs_overview of SSCS {
    title 'SSCS overview'
    description 'Services in SSCS and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view st_overview of ST {
    title 'ST overview'
    description 'Services in ST and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view wa_overview of WA {
    title 'WA overview'
    description 'Services in WA and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

  view xui_overview of XUI {
    title 'XUI overview'
    description 'Services in XUI and their immediate neighbours. Authentication and identity are omitted.'
    include
      *,
      -> * ->
    exclude
      element.tag = #ambient
    autoLayout LeftRight
  }

}
