compressionLevel: mixed

enableGlobalCache: false

nodeLinker: node-modules

# No `packageManager` field: it overrides yarnPath and makes Corepack re-download
# an identical Yarn on every CI run.
yarnPath: .yarn/releases/yarn-4.10.3.cjs
